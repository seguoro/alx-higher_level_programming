#!/usr/bin/python3


class Square:
    """Defines a square."""

    def __init__(self, side_length):
        """Constructor.
        Args:
            side_length: length of side of the square.
        """
        self.__side_length = side_length
